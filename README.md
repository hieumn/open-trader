# open-trader

Skeleton project for a technical‑indicator based trading agent.

## Quickstart (Linux)
1. Create venv and install:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt